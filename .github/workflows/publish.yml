name: Publish Docker Images

on:
  push:
    tags:
      - '[0-9]*'   # 1.0.0、0.2.0
      - 'v[0-9]*'  # v1.0.0
  workflow_dispatch: # 允许在 Actions 页面手动触发

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # 允许推送到 ghcr.io

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ghcr
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 阿里云 ACR（国内镜像源），需要在仓库 Secrets 配置 ACR_USERNAME / ACR_PASSWORD
      - name: Login to Aliyun ACR
        uses: docker/login-action@v3
        with:
          registry: crpi-auejvz24x1ul29s8.cn-guangzhou.personal.cr.aliyuncs.com
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      # ---------- 后端 ----------
      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./yunlan-video-server
          platforms: linux/amd64
          push: true
          provenance: false
          sbom: false
          tags: |
            ghcr.io/${{ github.repository_owner }}/aifusionvideo-server:${{ steps.version.outputs.VERSION }}
            ghcr.io/${{ github.repository_owner }}/aifusionvideo-server:latest
            crpi-auejvz24x1ul29s8.cn-guangzhou.personal.cr.aliyuncs.com/yunlance/aifusionvideo-server:${{ steps.version.outputs.VERSION }}
            crpi-auejvz24x1ul29s8.cn-guangzhou.personal.cr.aliyuncs.com/yunlance/aifusionvideo-server:latest
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend

      # ---------- 前端 ----------
      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./yunlan-video-webui
          platforms: linux/amd64
          push: true
          provenance: false
          sbom: false
          tags: |
            ghcr.io/${{ github.repository_owner }}/aifusionvideo-webui:${{ steps.version.outputs.VERSION }}
            ghcr.io/${{ github.repository_owner }}/aifusionvideo-webui:latest
            crpi-auejvz24x1ul29s8.cn-guangzhou.personal.cr.aliyuncs.com/yunlance/aifusionvideo-webui:${{ steps.version.outputs.VERSION }}
            crpi-auejvz24x1ul29s8.cn-guangzhou.personal.cr.aliyuncs.com/yunlance/aifusionvideo-webui:latest
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,mode=max,scope=frontend
