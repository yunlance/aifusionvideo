# 系统初始化 / 安装配置检查报告

- 日期：2026-08-28
- 范围：仅核对安装/部署相关配置，未改动任何源码（Java / TSX）
- 结论：主安装链路可用，无阻断性问题；唯一需要决策的是「已有一套实例时的容器命名冲突」（见第四节）

> 本文为辅助文件，文件名带 `_REVIEW_` 前缀，无需保留时可直接删除。

---

## 一、总体结论

`git clone` → `cp .env.example .env` → `docker compose up -d` 这条主安装流程是通的：

- Compose 引用的构建上下文、Dockerfile、Nginx 配置均真实存在；
- `.env.example` 覆盖了 Compose 引用的全部环境变量，无缺漏；
- 后端 Docker 配置与环境变量、前端 standalone 构建链路、Flyway 迁移 + 数据初始化顺序均一致；
- 本地开发链路（`application-local.yaml` + `docker-compose-middleware.yml` + `.env.development`）也一致。

---

## 二、已验证链路清单

| 检查项 | 涉及文件 | 结果 |
| --- | --- | --- |
| Compose 引用文件存在性 | `docker-compose.yml`、`docker-compose.build.yml`、`docker-compose.separated.yml` | 通过 |
| 后端 Dockerfile 多阶段构建 | `yunlan-video-server/Dockerfile`（含 ffmpeg/curl、健康检查 `/v3/api-docs`） | 通过，与 `springdoc` 路径一致 |
| 前端 Dockerfile standalone 构建 | `yunlan-video-webui/Dockerfile` + `next.config.ts`（`output: "standalone"`） | 通过 |
| pnpm 版本与 corepack | `package.json`（`pnpm@10.32.1`）、`pnpm-workspace.yaml` | 通过 |
| 运行时配置生成 | `docker-entrypoint.sh` → `write-runtime-config.mjs` → `public/runtime-config.js`；与 Nginx `location = /runtime-config.js` 匹配 | 通过 |
| Docker 后端环境变量 | `application-docker.yaml` 与 Compose `environment:` 一一对应（MYSQL_HOST/REDIS_HOST 等） | 通过 |
| `.env` 变量覆盖 | `.env.example` 与 Compose 引用的 `${VAR}` 全部匹配 | 通过 |
| `.env` 忽略规则 | 根 `.gitignore` 忽略 `/.env`，保留 `.env.example` | 通过 |
| `.dockerignore` | 前后端均正确排除 `target/`、`node_modules/`、`.next/`，不误删构建所需文件 | 通过 |
| Flyway 基线 + V 迁移 | `B1.1.0.0.8` 基线 + `V1.1.1.0.0` 起增量，版本递增无撞号（已与 `db/migration/README.md` 约定核对） | 通过 |
| 数据初始化顺序 | `DataInitializer`：先 `ensureAdmin()`（读 `ADMIN_PASSWORD`）→ 再幂等补演示数据 | 通过，新库/老库均成立 |
| Nginx 反代服务名 | `frontend:5000`、`backend:15858` 与 Compose 服务名一致 | 通过 |
| 本地开发代理 | `.env.development` 的 `DEV_BACKEND_URL` 与 `next.config.ts` 的 dev 校验一致 | 通过 |

---

## 三、关键初始化流程说明

1. **基础设施**：`docker compose up -d` 启动 mysql / redis，二者都带 healthcheck，后端 `depends_on: condition: service_healthy` 等其就绪。
2. **后端启动**：`SPRING_PROFILES_ACTIVE=docker` 加载 `application-docker.yaml`；Flyway 先建表种子（默认 AI 模型/网关走迁移写入）。
3. **数据初始化**：`DataInitializer` 在 Flyway 之后执行，创建 `admin`（密码取 `ADMIN_PASSWORD`，留空用内置默认哈希），并幂等补影视演示项目/剧本/分镜。
4. **前端启动**：入口脚本根据 `PUBLIC_API_URL` 生成 `public/runtime-config.js`；统一入口部署时留空即同源代理。

---

## 四、多实例并存 / 冲突分析（对应「已有一个项目会重复」）

### 冲突根因

当前 `docker-compose.yml` 与 `docker-compose.build.yml` 用了**硬编码 `container_name`**：

- `aifusionvideo-mysql`
- `aifusionvideo-redis`
- `aifusionvideo-backend`
- `aifusionvideo-frontend`
- `aifusionvideo-nginx`

Docker 的 `container_name` 是**宿主机全局唯一**，不受 `docker compose -p` 项目名隔离。因此在同一台 Docker 主机上，第二套实例（或原先已运行的这套）会因容器名重复而冲突。

另外两处硬编码也需要注意：

- MySQL 宿主机端口 `127.0.0.1:53306:3306`（`docker-compose.yml` / `docker-compose.build.yml` 均有）；中间件文件 `docker-compose-middleware.yml` 为 `53306:3306` 且带 `name: aifusionvideo`。
- Nginx 对外端口走 `APP_PORT`（可在 `.env` 改），尚可控。

### 不需要担心的部分

- **命名卷不冲突**：Compose 的 named volume 会按项目名自动加前缀（如 `<目录名>_mysql_data`）。只要两个实例所在目录名不同，`mysql_data` 等卷不会碰撞。
- **服务间互访不冲突**：Nginx `proxy_pass http://backend:15858`、`http://frontend:5000` 用的是 Compose 服务名，独立网络内各自解析，互不影响。

### 建议方案

**方案 A（推荐，配置级小改，不碰源码）**
去掉硬编码 `container_name:`，让 Compose 按项目名自动命名容器；同时把 MySQL 宿主机端口参数化到 `.env`（如 `MYSQL_PORT`）。这样同一台主机可并存多套实例、互不冲突，也更符合「任何人下载都能装」的目标。

- 改动仅限：`docker-compose.yml`、`docker-compose.build.yml`、`.env.example`（以及可选 `docker-compose-middleware.yml`）。
- 不影响 README 中的 `docker compose logs -f backend`（该命令基于服务名，不依赖 `container_name`）。

**方案 B（不改配置，立即验证）**
在独立机器 / 云主机 / 独立 Docker context（`docker context` 指向另一台 Docker）上做端到端验证，避开与现有实例的容器名与端口冲突。

> 若只是临时验证，也可先 `docker compose down` 停掉现有实例（**不要加 `-v`**，以免误删数据卷）。

---

## 五、待决策事项

1. 是否采用方案 A 修改 Compose 配置（参数化 `container_name` 与 MySQL 端口）。
2. 采用方案 B 时，验证环境（独立主机 or docker context）如何准备。
3. 后续是否需要以带前缀的方式落地一份「验证结果」文件。