# 进度跟踪：AI 配置 云揽川 锁定与品牌化

> 更新日期：2026-08-27

## 任务清单

| # | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| 1 | 梳理后端 AI 配置/模型接口与预设机制 | ✅ 完成 | ApiConfig / AiModel / ModelPreset |
| 2 | 前端：默认 云揽川、删常用提供商、图片/视频默认 newapi | ✅ 完成 | 文本协议默认仍是 `openai_compatible` |
| 3 | 前端：无配置时引导配置 云揽川 | ⚠️ 部分完成 | 无配置时显示「配置 云揽川 接入」；有任意配置后无法再新增 ComfyUI；空状态仍写「添加 API 配置」 |
| 4 | 前端：模型绑定白名单平台 | ✅ 完成 | 平台选项已收敛；**未删除**远程拉模型，`fetch-remote-models-dialog.tsx` 仍在使用 |
| 5 | 后端：API Key 保管、脱敏、编辑不覆盖 | ✅ 完成 | `ApiConfigConvert` 脱敏；update 忽略空/脱敏 Key |
| 6 | 后端：平台白名单、同平台唯一启用配置 | ✅ 完成 | `ALLOWED_PLATFORMS`；按 platform 唯一，不是全局只准 1 条 |
| 7 | 对照当前主干收口剩余入口与文案 | ⏳ 进行中 | 见下方遗留事项 |

## 变更记录

- 2026-08-23：创建需求目录，确认方案（A 层前端收敛 + B 层后端锁定）。
- 2026-08-23：完成前端默认值/删常用提供商/Key 不回显，以及后端 Key 脱敏/白名单/同平台唯一配置。
- 2026-08-27：对照当前主干修正文档。确认远程拉模型仍保留；新建入口在已有配置后消失；产品名是云揽镜、网关品牌是云揽川。

## 遗留事项

- 已有云揽川配置后，页面没有「再添加 ComfyUI」入口，与「允许 云揽川 + ComfyUI」不一致。
- 空状态文案仍是「点击上方「添加 API 配置」开始」，实际按钮是「配置 云揽川 接入」。
- 文本协议默认 `openai_compatible`，与计划中的 `newapi` 不一致，需产品确认。
- 云揽川卡片仍提供远程拉模型。若产品要完全锁定预置模型，应去掉该入口；若作为填 Key 后的发现能力，应把计划改成保留。
- 种子数据 `V1.1.3.0.0` / `V1.1.3.0.1` 尚未提交；若合入，首次启动会预置 `gate.xinchyun.com` 网关和默认模型。
