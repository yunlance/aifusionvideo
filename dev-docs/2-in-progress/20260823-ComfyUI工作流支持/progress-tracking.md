# ComfyUI 对接计划执行进度

## 使用规则

- 每完成一个阶段立即更新状态、完成证据、验证命令与下一步。
- 状态只使用：`未开始`、`进行中`、`已完成`、`受外部环境限制`。
- “已完成”必须有代码或命令证据；本地没有 ComfyUI 时不得把真实联调标记为完成。

## 总体状态

| 阶段 | 状态 | 交付物 |
| --- | --- | --- |
| A. 调研与顶层文档 | 已完成 | 三份文档（目录 `20260823-ComfyUI工作流支持/`） |
| B. 工作流领域与数据库 | 已完成 | `V1.1.1.0.0`、实体、CRUD、版本、发布 |
| C. Native API 与工作流渲染 | 已完成 | Client、DTO、Renderer、导入校验 |
| D. 图片/视频生成接入 | 已完成 | Strategy、同步轮询、取消、结果入库 |
| E. 前端管理闭环 | 已完成 | 连接测试、导入绑定、版本发布、模型关联 |
| F. 真实环境验收 | 受外部环境限制 | 代码已在主干；MySQL validate 与 GPU 联调待环境 |

当前仓库是云揽镜 v1.0.0 基线，ComfyUI 工作流实现已经在主干里。阻塞式 `Thread.sleep` 轮询的改造是独立待办，不阻塞本需求标记为“代码已交付”。

## 阶段 A：调研与顶层文档

### 子任务

- [x] 确认 Native API v0.30.0 路由与任务状态契约。
- [x] 制定数据模型、版本策略、用户流程和运行时流程。
- [x] 建立开发实施手册、执行进度文档、技术债务文档。
- [x] 对照当前主干架构复核文档（2026-08-27）。

### 完成证据

- 文档目录：`dev-docs/2-in-progress/20260823-ComfyUI工作流支持/`。
- 迁移：`V1.1.1.0.0__comfyui_workflow_support.sql` 已在主干。

## 阶段 B：工作流领域与数据库

### 子任务

- [x] 创建 `V1.1.1.0.0__comfyui_workflow_support.sql`。
- [x] 创建工作流与版本实体、Mapper。
- [x] 创建 CRUD、导入、发布、回滚 Service。
- [x] 创建管理 Controller 与 VO。
- [x] 扩展模型和任务的工作流引用。
- [x] 增加缓存注解及写操作缓存失效。
- [x] 完成领域单元测试和 Flyway 命名测试。

## 阶段 C：Native API 与工作流渲染

### 子任务

- [x] 实现连接检测、节点/模型查询。
- [x] 实现图片上传、任务提交、查询、取消和文件下载。
- [x] 实现 Bearer Token 与现有出站代理。
- [x] 实现 API-format 静态校验、依赖提取和哈希。
- [x] 实现类型安全输入绑定和显式输出解析。
- [x] 以 JDK HTTP stub 完成官方契约测试。
- [x] 对照官方 v0.30.0 终审响应字段、状态与取消语义。
- [x] 删除协议响应字段的非官方 fallback，并为未知长度 JSON、输入图片和输出增加读取/写入硬上限。

## 阶段 D：图片/视频生成接入

### 子任务

- [x] 增加 `comfyui` 图片和视频策略。
- [x] 模型路由支持 `modelProtocol=comfyui`。
- [x] 提交时固化工作流版本。
- [x] 上传图片输入并渲染工作流；视频/音频上传绑定明确拒绝。
- [x] 轮询、超时、取消、错误映射（当前为消费线程内同步轮询）。
- [x] 下载并保存图片、视频和封面。
- [x] 完成图片/视频策略与 Consumer 测试。

## 阶段 E：前端管理闭环

### 子任务

- [x] API 配置增加 ComfyUI 与连接测试。
- [x] 新增工作流列表与六步导入向导。
- [x] 新增输入绑定与输出绑定编辑器。
- [x] 新增版本、验证、试运行、发布、回滚和草稿删除操作。
- [x] 模型编辑器支持关联 ComfyUI 工作流并同步参考图能力。
- [x] 生成历史支持取消排队任务和运行中的 ComfyUI 任务。
- [ ] 已有云揽川配置后，页面仍能新增 ComfyUI 配置（当前新建入口会消失，见 NewAPI 锁定技术债）。

## 阶段 F：真实环境验收

### 子任务

- [x] 主干包含 ComfyUI 后端与前端实现。
- [ ] 启动目标 MySQL 完成 Flyway validate 与全量测试。
- [ ] 使用真实 ComfyUI v0.30.0 或用户实例完成 GPU 联调。
- [ ] 异步轮询改造（独立待办，完成后从技术债划出）。

## 变更日志

### 2026-08-27

- 对照云揽镜当前主干修订文档：ComfyUI 代码已在仓库中，不再依赖本仓库不存在的 `v-1.1.0` / `origin/main`。
- 明确同步轮询仍占用消费线程；异步方案拆到 `1-todo/2026-08-23-ComfyUI任务异步轮询.md`。
- 记录 AI 配置页在已有云揽川后无法再添加 ComfyUI 的入口缺口。

### 2026-08-04

- 上游完成官方 API 调研、三份文档、领域/Native API/生成策略/前端六步工作流管理。
- 官方 v0.30.0 协议终审：上传、下载、节点定义、系统版本、Jobs 输出/状态/取消契约。
- 工作流管理入口下沉到对应 ComfyUI 供应商卡片，按 `apiConfigId` 隔离。
